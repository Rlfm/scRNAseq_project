```{r}
# load data
data_dir <- getwd() |>
  substr(1, nchar(working_dir)-2) |>
  paste("data", sep = "/")

path_to_object <- gsub(" ", "",paste(data_dir,"/retina.rds"))
path_to_object
lens.data <- readRDS(path)



```

```{r}

lens.data[["percent.mt"]] <- PercentageFeatureSet(lens.data, pattern = "^MT-")
lens.data[["percent.hb"]] <- PercentageFeatureSet(lens.data, pattern = "^HB")

VlnPlot(lens.data, features = c("nFeature_RNA", "nCount_RNA", "percent.mt","percent.hb"), ncol = 4)
```